

# 
# Pow-ize the current directory, with config files


[[ -a config.ru ]] || return

cd ~/.pow
ln -s $OLDPWD
cd $OLDPWD

echo "rvm use default@`basename $PWD`" > .rvmrc
echo "\n.powrc\n.rvmrc\n" >> .gitignore
touch .powrc
